/*
 * Data_test.cpp
 *
 *  Created on: Oct 14, 2017
 *      Author: sammie
 */
#include <gtest/gtest.h>
#include <vector>
#include <string>
#include <iostream>
#include "Data.h"

TEST(Data_test, testReadWeightsFromFile) { //Need to Fix Test Names!
  // Using test file make sure input weights match what's in file
}

TEST(Data_test, testReadDataFromFile) { //Need to Fix Test Names!
  // Using test file make sure data match what's in file
}

TEST(Data_test, testReadTopologyFromFile) { //Need to Fix Test Names!
  // Using test file make sure topology matches what's in file
}

TEST(Data_test, testWriteWeightsToFile) { //Need to Fix Test Names!
  // Using test file make sure new weights are written to file
}

TEST(Data_test, testEditTrainingDataInFile) { //Need to Fix Test Names!
  // Using test file make sure case is removed from all test data
}

